services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: anidex
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    # develop:
    #   watch:
    #     - action: sync
    #       path: .
    #       target: /app
    #       ignore:
    #         - node_modules
    #         - .next
  redis:
    image: redis:alpine
    container_name: redis
    ports:
      - "6379:6379"